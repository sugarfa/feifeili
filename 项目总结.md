# 个人网页项目 · 会话总结

> 会话日期：2026-09-23
> 仓库：`https://github.com/sugarfa/feifeili.git`（分支 `main`）
> 线上站点：**https://sugarfa.github.io/feifeili/**
> 本文档目的：记录本次会话的目标、操作过程、关键问题与解决方案，便于日后复现与分析。

---

## 一、会话目标

1. 根据《李飞飞-个人简历.pdf》生成个人网页介绍（静态单页，纯 HTML/CSS/JS，无外部依赖）
2. 将代码推送至 GitHub 仓库 `sugarfa/feifeili` 并开启 **GitHub Pages** 发布
3. 依据概普任职期间的「项目报告」（`项目报告/`）与参与分析的论文（`paper/`）完善页面的项目经验与科研成果板块

## 二、最终成果概览

### 2.1 交付物

| 项 | 值 |
|---|---|
| 线上地址 | https://sugarfa.github.io/feifeili/ |
| 部署方式 | GitHub Pages（legacy 模式，直接从 `main` 分支根目录发布，无构建步骤） |
| 页面技术 | 原生 HTML + CSS + JS（无框架、无 CDN、无外部字体依赖） |
| 最后提交 | `5914973`（推送时确认） |

### 2.2 页面内容结构（自上而下）

| 区块 | 内容 |
|---|---|
| 顶部导航 | 关于 / 教育背景 / 工作经历 / 项目经验 / 科研成果 / 专业技能 / 联系我（移动端汉堡菜单） |
| Hero | 姓名、求职意向（生物信息工程师）、现居地、技术标签、打字机问候语 |
| 关于我 | 自我介绍、基本信息卡（年龄 27、中共党员、CET-4 524 / CET-6 486、优秀毕业生）、事实数据（8 篇论文 / 30+ 项目 / 3 段经历 / 10+ 方法） |
| 教育背景 | 时间线：河北大学理学硕士（2020.09–2023.06，生物化学与分子生物学·生信方向）、理学学士（2016.09–2020.06，生物信息学） |
| 工作经历 | ① 安诺优达 · 生物信息工程师（2026.02 至今）② 北京概普生物 · 生物信息工程师（2023.11–2026.01）③ 北京元码医学 · 微生物专项研究员（2023.06–2023.11） |
| 项目经验 | 统计卡片（30+ 项目 / 12+ 方法 / 15+ 疾病 / 6 类组学）、分析方法标签云（13 项）、疾病领域标签云（22 项）、精选项目案例 6 个（各含「成果」摘要） |
| 科研成果 | **署名论文 4 篇**（含期刊、IF、作者排序）+ **参与分析论文 4 篇**（标注「分析」角色，无署名）+ 河北大学研究生创新项目 |
| 专业技能 | 生物信息分析 / 编程与工具 / 科研素养三卡 |
| 自我评价 | 三卡片（性格、科研素养、团队） |
| 联系我 | 邮箱 / 电话 / 现居 / GitHub 四入口 |
| 页脚 | 版权 + "Powered by GitHub Pages" |

### 2.3 科研成果列表（页面最终版）

**署名论文：**
1. Prognostic stratification of gastric cancer patients by intratumoral microbiota-mediated tumor immune microenvironment. *Microbial Pathogenesis*, IF 3.3, 2025, 二作（元码基因 Geneis 任职期间）
2. MdSVWC1, a new pattern recognition receptor triggers multiple defense mechanisms against invading bacteria in *Musca domestica*. *BMC Biology*, IF 4.4, 2024, 六作
3. E2F 家族转录因子在肿瘤发生中的作用.《遗传》, 中文核心, 2023, 一作
4. Single von Willebrand factor C-domain protein confers host defense against white spot syndrome virus…. *IJBM*, IF 8.2, 2023, 四作

**参与分析论文（概普期间，无署名）：**
1. Integrative analysis of m6A-SNPs and single-cell RNA sequencing reveals key drivers of endocrine combined with CDK4/6 inhibitor therapy resistance in ER+ breast cancer. *Frontiers in Pharmacology*, 2025
2. Landscape of the intratumoral microbiota acting on the tumor immune microenvironment in LUAD and LUSC. *Physiological Genomics*, 2025
3. Multi-omics integration reveals the role of N6-methyladenosine in epilepsy, ischemic stroke, and vascular dementia. *Molecular Brain*, 2025
4. Tumor-colonizing Lachnoclostridium-mediated chemokine expression enhances the immune infiltration of bladder urothelial carcinoma. *Cancer Immunology, Immunotherapy*, 2025

> ⚠️ 后 4 篇的 IF 为当日按 2024 JCR 填写的近似值（IF≈4.4 / 2.5 / 3.5 / 4.6），如与实际有出入需在 `index.html` 的 `pub-meta` 中修正。

---

## 三、时间线与操作记录

### 第 1 步：简历解析与页面构建
- 用 `pdfplumber` 提取《李飞飞-个人简历.pdf》全文（求职意向、教育、工作、科研、技能、自我评价）
- 手工构建 `index.html`（~800 行）、`assets/css/style.css`（含响应式 + 入场动画）、`assets/js/main.js`（导航、返回顶部、IntersectionObserver 动画、打字机效果）
- 校验：HTML 结构（自定义 HTMLParser）、CSS 花括号配对、`node --check` JS 语法
- 提交 `b1365ad`

### 第 2 步：发布尝试与网络问题（本会话最大障碍）
**现象**：`git push` 报 `OpenSSL SSL_read: SSL_ERROR_SYSCALL` / `Failed to connect to github.com port 443 after 128786 ms`

**诊断结论**：DNS 解析 `github.com → 20.205.243.166`（新加坡节点），**该 IP 被本网络屏蔽**；测试多个 IP：
- `140.82.112.4 / 140.82.113.4 / 140.82.114.4 / 20.27.177.113` 均可访问（HTTP 200）
- `20.205.243.166` 超时

**绕过方案（HTTPS）**：repo 本地配置强制解析
```bash
git config http.https://github.com/.curloptResolve "github.com:443:140.82.112.4"
# 写入 .git/config（不进入版本库），可持续生效
```

**认证问题**：首个 token（fine-grained，`github_pat_…`）只能读（`git-upload-pack` 200），写被拒（`git-receive-pack` 403 "Permission denied to sugarfa"）——fine-grained token 默认 Contents 只读。
→ 用户改提供经典 token（`ghp_…`，勾选 `public_repo`），API 校验 `login: sugarfa`。

**最终方案（SSH，推荐）**：SSH 22 端口连通稳定，且本机 `~/.ssh/id_rsa` 已注册到 GitHub 账户（key 标题 `anno`，`ssh -T git@github.com` 显示 `Hi sugarfa! You've successfully authenticated`）。
```bash
git remote set-url origin git@github.com:sugarfa/feifeili.git
git push origin main   # 一次成功
```

### 第 3 步：开启 GitHub Pages（API 方式）
```bash
curl -X POST -H "Authorization: Bearer $TOKEN" -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/sugarfa/feifeili/pages \
  -d '{"source":{"branch":"main","path":"/"}}'
```
- 响应 `build_type: legacy`、`html_url: https://sugarfa.github.io/feifeili/`
- 经典 token（public_repo）可调用该接口；fine-grained 需 **Pages: Read and write** + **Contents: Read and write**
- legacy 模式 = 直接从分支目录提供静态文件（`.nojekyll` 防 Jekyll），**每次 push main 即自动更新页面**，无需 workflow

### 第 4 步：迭代 1——工作经历更新
- 用户提供最新信息：概普生物 **2023.11–2026.01**，**安诺优达 2026.02 至今**（原简历写"概普至今"）
- 调整三张经历卡片顺序（安诺优达在前）、“关于我”事实数字（2→3 段经历），提交 `7e9b1f6`

### 第 5 步：迭代 2——项目报告与论文补充
用户放入资料：`项目报告/`（31 份概普客户分析报告 docx）和 `paper/`（8 篇论文 PDF）。

**信息提取**：
- 4 篇**新**论文（不在简历中），用 pdfplumber 提取首页标题/期刊/DOI，并全文检索确认**无 "Feifei Li" 署名** → 按"参与分析"展示
- 用户澄清重要事实：**Geneis Beijing = 元码基因**（不是概普）；概普项目只负责分析、**不署名**
- 31 份 docx 报告按文件名分类统计方法与疾病领域

**页面改动**（提交 `1accdd2`）：
- 新增「项目经验」板块：统计卡、方法/疾病标签云、精选案例 6 个
- 科研成果新增「参与分析论文」分组（4 篇，徽章为琥珀色「分析」样式 `.pub-author.analysis`）
- 更新导航、关于我数据（8 篇论文 / 30+ 项目 / 10+ 方法）、概普经历描述（"累计完成 30+ 项项目"）
- **新增 `.gitignore` 排除 `项目报告/` 与 `paper/`**（涉密与版权材料，约 260MB，不 push）

### 第 6 步：迭代 3——精选项目补充「成果」
用 `python-docx` 读取 6 份对应报告的结论段（`结果：`/`结论`），为每个精选项目卡片新增 `.proj-result` 段落（忠实概括报告结论），提交 `5914973`：

| 卡片 | 成果核心 |
|---|---|
| 卵巢癌 RNA 修饰耐药 F241011002 | 23 个 RNA 修饰基因；TGFβ 通路 + 纤维细胞 TIPARP 介导耐药 |
| 结肠癌超级增强子 F241119002 | CEACAM1 / S100A11 预后模型；增强子-转录因子-靶基因轴 |
| NSCLC m6A 耐药 F240523002 / F240528002 | 上皮亚群 IC50↑、谷胱甘肽代谢/铁死亡；巨噬细胞 GAL3ST4 + 脂肪酸降解 |
| 前列腺癌 TME F240718001 | 治疗后 MDSC↑、MHC class I↑；WASHC4、C2 保护性预后基因 |
| 自闭症 MR F231018002 | IFN-γ、TGFβ 与自闭症存在潜在因果关联 |
| 胰腺癌肝转移 ecDNA F250707004 | Malignant_C1（52%）；预后模型 5 数据集验证（HR=2.43） |

### 第 7 步：持续验证
每次 push 后 `curl -sL https://sugarfa.github.io/feifeili/` + `grep` 关键词校验（HTTP 200，新内容均在）。

---

## 四、关键命令速查

```bash
# 推送（SSH）
cd /annogene/data2/bioinfo/PMO/feifeili/github/feifeili
git push origin main

# HTTPS 网络绕过（如换机器重配）
git config http.https://github.com/.curloptResolve "github.com:443:140.82.112.4"

# 检查 SSH 认证
ssh -T git@github.com

# 本地预览
python3 -m http.server 8000   # http://localhost:8000

# 校验工具（本会话用到的）
node --check assets/js/main.js
python3 - <<'EOF'  # 简易 HTML 结构检查
from html.parser import HTMLParser
print("（见会话历史）")
EOF

# Pages 状态检查
curl -s -H "Authorization: Bearer $TOKEN" \
  https://api.github.com/repos/sugarfa/feifeili/pages/builds/latest
```

---

## 五、信息源 → 页面内容映射

| 信息源 | 用途 |
|---|---|
| `李飞飞-个人简历.pdf` | 基本信息、教育、署名论文（含 IF/作者序）、技能、自我评价 |
| `项目报告/*.docx`（31 份） | 项目经验板块：统计、方法与疾病标签、6 个精选案例及其成果 |
| `paper/*.pdf`（8 篇） | 科研成果：4 篇署名 + 4 篇参与分析（元码/概普确认） |
| 用户口头补充 | ① 工作经历时间更正（概普 2023.11–2026.01、安诺优达 2026.02 至今）② Geneis=元码、概普不署名 ③ 安诺优达职责草稿（"生物信息工程师"，一般性描述，待确认） |

## 六、文件结构（当前仓库）

```
feifeili/
├── index.html                # 主页（全部内容）
├── assets/css/style.css      # 样式（约 700 行，含响应式）
├── assets/js/main.js         # 交互（导航/返回顶部/动画/打字机）
├── README.md                 # 项目说明 + 发布指南
├── .nojekyll                 # 禁用 Jekyll
├── .gitignore                # 排除 项目报告/ 与 paper/
├── 李飞飞-个人简历.pdf        # 简历原件（已入库）
├── 项目报告/                 # 本地仅存（gitignored，涉密）
└── paper/                   # 本地仅存（gitignored，版权）
```

样式要点：CSS 变量主题（teal 渐变 + amber 点缀）、卡片 hover 微动效、`.reveal` 入场动画、时间线、断点 900px / 640px。

## 七、安全性说明（重要）

1. **Token 已暴露**：`github_pat_…` 与 `ghp_…` 曾出现在对话中。**建议用户尽快在 GitHub → Settings → Developer settings → Personal access tokens 中吊销并重生**。日常更新用 SSH（无需 token）。
2. **`项目报告/` 涉密**：属于客户交付报告，严禁发布。已通过 `.gitignore` 排除；**不要** `git add -f` 强行提交。
3. **`paper/` 版权**：期刊 PDF 版权归出版社，仅本地提取信息使用。
4. **仓库中已提交 `李飞飞-个人简历.pdf`**：如不希望公开简历原件，可联系用户移除（`git rm` + 历史清理，或直接删除文件后 push）。

## 八、从零复现步骤

1. **建站**：编写 `index.html` + `assets/`，内容参考简历/报告/论文（见第五节映射）
2. **校验**：HTML 结构、CSS 括号、JS 语法
3. **推送**：
   - SSH 可用则：`git remote set-url origin git@github.com:sugarfa/feifeili.git && git push origin main`
   - 仅 HTTPS 则：配置 `curloptResolve` 绕过被屏蔽的解析 IP，用带 `public_repo` 的 token 推送
4. **开 Pages**（若未开）：POST `/repos/{owner}/{repo}/pages`，body `{"source":{"branch":"main","path":"/"}}`
5. **验证**：等待约 15–30s（legacy 自动构建），`curl -sL` + 关键词 grep

## 九、已知问题与后续改进建议

- [ ] 4 篇参与分析论文的 IF 需按最新 JCR 核实
- [ ] 安诺优达任职的职位/职责描述为草稿，待用户确认后更新
- [ ] 「关于我」事实数字（8 篇论文）为动态内容，新增/删减论文时需同步
- [ ] 可选：移除已入库的简历 PDF、为页面加英文版、加博客/作品展示
- [ ] 可选：把网络绕过方案整理进 README，换机器可快速复现
- [ ] 安全：吊销本次会话中出现的两个 token

---

*文档生成时间：2026-09-23 · 配合本人个人网页项目使用*